---
# ============================================================================
# ziti_controller: core instance identity
# File: host_vars/core/ziti_controller.yaml
# ============================================================================
#
# Locked identity values (see P06 Roadmap) - permanent once the controller
# is bootstrapped. Changing either requires full PKI regeneration and
# re-enrollment of every identity in the overlay.

ziti_ctrl_advertised_address: "zctrl1.neox5.com"
ziti_controller_id: "zctrl1"

ziti_admin_password: "{{ ziti_admin_password_secret }}"
