---
# =============================================================================
# Security: n150 Group Configuration
# File: group_vars/n150/security.yml
# Role: security_hardening
# =============================================================================
#
# Firewall ports required for reverse proxy operation.
# Caddy listens on 80 (HTTP redirect) and 443 (HTTPS).
#
# Applied to: All hosts in 'n150' group
# Variable precedence: all < n150 (this file) < host_vars

security_firewall_allowed_tcp_ports:
  - 80
  - 443
