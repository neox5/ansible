# {{ ansible_managed }}
[Unit]
Description=Restic {{ item.value.type }}: {{ item.key }}
After=network-online.target

[Service]
Type=oneshot
User={{ restic_user }}
ExecStart={{ restic_script_dir }}/{{ item.value.type }}-{{ item.key }}.sh

[Install]
WantedBy=multi-user.target
