---
# Start tasks - enable and start this instance's controller service

- name: Enable and start ziti-controller instance service
  ansible.builtin.systemd:
    name: "ziti-controller@{{ ziti_controller_id }}"
    state: "{{ ziti_controller_service_state }}"
    enabled: "{{ ziti_controller_service_enabled }}"
    daemon_reload: true
