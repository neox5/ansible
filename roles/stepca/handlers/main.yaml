---
# stepca role handlers

- name: restart stepca
  ansible.builtin.systemd:
    name: stepca
    state: restarted
    daemon_reload: true

- name: reload systemd
  ansible.builtin.systemd:
    daemon_reload: true
