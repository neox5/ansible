---
# Certs role - Main task orchestration
# Debian 13 Trixie only

- name: Include preflight checks
  ansible.builtin.import_tasks: preflight.yaml
  tags:
    - certs
    - certs-preflight

- name: Include deployment tasks
  ansible.builtin.import_tasks: deploy.yaml
  tags:
    - certs
    - certs-deploy
