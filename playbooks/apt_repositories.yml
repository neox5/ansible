---
# APT Repositories Configuration
# Usage: ansible-playbook -i inventory/lab playbooks/apt_repositories.yml

- name: Configure APT repositories
  hosts: all
  become: yes
  roles:
    - apt_repositories
